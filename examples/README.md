# Examples

These examples use the reserved `.test` domain and synthetic values only:

- `evidence-observed.json` demonstrates successfully collected static evidence.
- `evidence-blocked.json` preserves a blocked collection state without pretending the page was absent.

Both identify their evidence schema version and are checked by the release validator. They are contract
fixtures, not claims about a live website and not performance benchmarks.
